# 介護校ファネルダッシュボード

ホリエモンAI学校 介護校のリード→商談→成約を週次集計して公開している静的サイト。

- 公開URL: https://hideyoshihommura-design.github.io/kaigo-funnel-dashboard/
- リポジトリ: `hideyoshihommura-design/kaigo-funnel-dashboard`（**public**）
- 閲覧者は社内外。パスワードなし。

## 構成

| パス | 役割 |
|---|---|
| `scripts/fetch_data.py` | HubSpot + Googleスプレッドシート → `data.json` |
| `scripts/build_dashboard.py` | `data.json` → `index.html`（標準ライブラリのみ。Chart.jsは`assets/`から埋め込む） |
| `scripts/postprocess.py` | noindex・PWA・「生成日」→「数字の更新」の置換 |
| `config/channel_map.json` | 流入経路→チャネル（event/web/line/referral/other/agency）、除外経路 |
| `config/webinars.json` | ウェビナー各回の掲載期間 |
| `config/ad_campaigns.json` | 広告の媒体・キャンペーン定義とウェビナー区分 |
| `config/callers.json` | 架電業者に渡しているHubSpotアカウント（owner_id） |
| `data.json` / `index.html` | **生成物。手で編集しない** |

`check_hubspot_access.py` と `probe_ads.py` は読み取り専用の調査用。

## 変更のしかた

1. `scripts/` か `config/` を直す。`index.html` を直接いじっても次の自動実行で消える。
2. 認証情報なしで見た目だけ確認できる:
   ```
   python scripts/build_dashboard.py data.json -o /tmp/preview.html
   ```
   ただし **2026-09-02 時点でこのPCにPythonが入っていない**（`python.exe` は
   Microsoft Store のスタブだけ）。構文チェックすらローカルでできないので、
   `scripts/` を直したら Actions のログで確認すること。
   `fetch_data.py` はこのPCでは動かない（キーは GitHub Secrets にしか無い）。
3. commit → push → `gh workflow run update-dashboard.yml` → 完了を待つ →
   公開ページのタイムスタンプが変わったことまで確認する。

自動実行は3時間ごと（cron `5 */3 * * *`, UTC）。GitHub側に間引かれることがある。

## 集計上の決めごと（過去に間違えた箇所）

- **広告データは「広告日次シート」1枚だけを読む。** 旧「週次分析テンプレ(1年分)」は
  もう参照していない。あちらは火曜始まりの週次タブと日次入力タブが混在し、日次が
  2026-06-09 開始だったため、それ以前は日単位で切れなかった。新シートは配信初日
  （2026-04-07）から全期間が日次で、1行 = 1日 × 1媒体 × 1キャンペーン。
- **CPLの分母は広告日次シートのCV数。** HubSpotのユニーク連絡先ではない。
  同じ人が複数回フォームを出すため、ユニークだと分母が小さくなりCPLが跳ね上がる。
  ページ内のCPLは全部この分母で揃える。
- **ウェビナー別の集計は「キャンペーン列 == ウェビナー」の行だけ。媒体はまたぐ。**
  Meta / Google / LINE のどれにもウェビナー配信があった（2026年4月）。キャンペーン名の
  部分一致で判定してはいけない。全キャンペーンを足すと費用が倍以上になる。
  総web費用と全体CVのほうは、媒体・区分に関係なく全行を合計する。
- **取り消された取引は成約に数えない。** `closedwon` に入った履歴があっても、
  現在のステージが `closedwon` でなければ除外する。
- **ウェビナー各回の期間は重ねない。** 掲載期間が重複する回は境界で切る。
  広告費とCVは広告日次シートから期間で切って自動計算する。手入力の上書きは廃止した
  （第1〜3回に入っていたがMeta分しか含んでおらず、Google/LINEのウェビナー配信が
  抜けていた）。
- **架電由来は「直前にかけた人」で判定する。** 面談予約・商談・成約が起きた日に対して、
  そのコンタクトへ**その日以前で最後にかけた**コールの `hubspot_owner_id` を見る。
  業者アカウントは `config/callers.json`。**owner_id だけを書き、氏名は書かない**
  （公開リポジトリなので。IDと氏名の対応はHubSpot側で引く）。IDで判定するので改名でも壊れない。
  初回架電者で決めてはいけない。社内が一度かけて不発だった相手を業者が掘り起こした分が
  社内の成果になり、業者の実績が過小に出る。
  日付（`VENDOR_START` 以降）で業者ぶんとみなしてはいけない。IS活動量の架電業者カードは
  日付固定のままだが、あれは業者アカウントしか架電していない期間だから成立している。
  社内が1件でも架電すれば崩れる。
- **架電由来の母集団はFS指標と同じ**（直契約・route有効・獲得週が期間内）。
  業者由来＋社内由来＋架電なし＝FS活動量の同じ指標になる。
  面談予約はIS活動量の「面談予約 獲得数」とは一致しない（あちらはコンタクト未紐付けの
  取引と代理店も含む）。
- **由来の判定に使う架電は、期間も `hs_call_direction` も絞らない。**
  `hs_call_direction` が空のコールが22件ある（CRM_UIに手入力したもの）。IS活動量の
  架電数はこれを落としているが（発信と判定できないため）、由来の判定で落とすと
  その架電から生まれた商談が「架電なし」に回る。実際に面談予約2件（業者の架電あり）が
  漏れていた。着信を含めても取り違えにはならない。業者アカウントに折り返しが来ている
  なら、接触しているのは業者だからである。
- **架電由来は `PERIOD_START` から出す。`CALLS_START` で切らない。**
  介護校の架電（`CRM_UI`）は全期間で600件弱、**最初が 2026-05-12**。2025年9月〜
  2026年4月は1件も無い。だからその期間の商談が「架電なし」に並ぶのは事実であって
  データ欠けではない。`CALLS_START` はIS活動量ブロックの表示開始日でしかないので、
  ここで由来を切ると「9月から取引があるのに6月からしか出ない」ことになる。
  架電は開始日で切らず全期間取る（2026-05の3件を落としていた）。
  5月以前のコールが68,130件あるのは全部 `INTEGRATION`（Kintone移行で入った
  介護施設運営側）で、介護校のものではない。`CALL_SOURCE=CRM_UI` で除外している。
- **FS指標は直契約のみ**（`ch != "agency"`）。全期間にすると上部の総商談数と一致する必要がある。
- 商談はコンタクト獲得週のコホート、FS指標はステージ到達日ベース。
- 率は加重（合計÷合計）。週ごとの率を平均しない。

主な定数: `PERIOD_START=2025-09-01` / `CALLS_START=2026-06-01` /
`VENDOR_START=2026-08-19`（架電業者稼働開始）。広告データの開始日は定数で持たず、
シートに入っている最初の日をそのまま使う（2026-04-07）。

## 表示の決めごと

- **分析コメント・注釈・改善提案は書かない。数字だけ。**
- 用語: 転換率ではなく**商談化率**。面談予約は**面談予約 獲得数**。
- インサイドセールス（架電）とフィールドセールス（面談）は別セクション。
- セクション順: リード獲得 → ウェビナー別 → IS活動量 → FS活動量 → 架電由来 → 直契約 → 代理店 → 展示会別CPL
- 折りたたみは既定で閉じる。開閉状態は localStorage で保持。
- 期間指定を変えたら各カードの数値も再計算されること（架電業者カードは対象外）。

## 触ってはいけないこと

- **トークンやキーの発行・入力をこちらでやらない。** ユーザー本人が GitHub Secrets に入れる。
  中身を見せてもらう必要もない。
- 公開リポジトリなので、認証情報をコミットしない（`.gitignore` で名前を弾いている）。
- 公開ページから再集計を起動するボタンは作れない（トークンが露出する）。
