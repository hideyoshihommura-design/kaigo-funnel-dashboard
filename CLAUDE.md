# 介護校ファネルダッシュボード

ホリエモンAI学校 介護校のリード→商談→成約を週次集計して公開している静的サイト。

- 公開URL: https://hideyoshihommura-design.github.io/kaigo-funnel-dashboard/
- リポジトリ: `hideyoshihommura-design/kaigo-funnel-dashboard`（**public**）
- 閲覧者は社内外。パスワードなし。

## 構成

| パス | 役割 |
|---|---|
| `scripts/fetch_data.py` | HubSpot + Googleスプレッドシート → `data.json` |
| `scripts/build_dashboard.py` | `data.json` → `index.html`（標準ライブラリのみ。Chart.jsは`assets/`から埋め込む） |
| `scripts/postprocess.py` | noindex・PWA・「生成日」→「数字の更新」の置換 |
| `config/channel_map.json` | 流入経路→チャネル（event/web/line/referral/other/agency）、除外経路 |
| `config/webinars.json` | ウェビナー各回の掲載期間 |
| `config/ad_campaigns.json` | 広告の媒体・キャンペーン定義とウェビナー区分 |
| `config/callers.json` | 架電業者に渡しているHubSpotアカウント（owner_id） |
| `data.json` / `index.html` | **生成物。手で編集しない** |

`check_hubspot_access.py` と `probe_ads.py` は読み取り専用の調査用。

## 変更のしかた

1. `scripts/` か `config/` を直す。`index.html` を直接いじっても次の自動実行で消える。
2. 認証情報なしで見た目だけ確認できる:
   ```
   python scripts/build_dashboard.py data.json -o /tmp/preview.html
   ```
   ただし **2026-09-02 時点でこのPCにPythonが入っていない**（`python.exe` は
   Microsoft Store のスタブだけ）。構文チェックすらローカルでできないので、
   `scripts/` を直したら Actions のログで確認すること。
   `fetch_data.py` はこのPCでは動かない（キーは GitHub Secrets にしか無い）。
3. commit → push → `gh workflow run update-dashboard.yml` → 完了を待つ →
   公開ページのタイムスタンプが変わったことまで確認する。

自動実行は3時間ごと（cron `5 */3 * * *`, UTC）。GitHub側に間引かれることがある。

## 集計上の決めごと（過去に間違えた箇所）

- **広告データは「広告日次シート」1枚だけを読む。** 旧「週次分析テンプレ(1年分)」は
  もう参照していない。あちらは火曜始まりの週次タブと日次入力タブが混在し、日次が
  2026-06-09 開始だったため、それ以前は日単位で切れなかった。新シートは配信初日
  （2026-04-07）から全期間が日次で、1行 = 1日 × 1媒体 × 1キャンペーン。
- **CPLの分母は広告日次シートのCV数。** HubSpotのユニーク連絡先ではない。
  同じ人が複数回フォームを出すため、ユニークだと分母が小さくなりCPLが跳ね上がる。
  ページ内のCPLは全部この分母で揃える。
- **ウェビナー別の集計は「キャンペーン列 == ウェビナー」の行だけ。媒体はまたぐ。**
  Meta / Google / LINE のどれにもウェビナー配信があった（2026年4月）。キャンペーン名の
  部分一致で判定してはいけない。全キャンペーンを足すと費用が倍以上になる。
  総web費用と全体CVのほうは、媒体・区分に関係なく全行を合計する。
- **取り消された取引は成約に数えない。** `closedwon` に入った履歴があっても、
  現在のステージが `closedwon` でなければ除外する。
- **ウェビナー各回の期間は重ねない。** 掲載期間が重複する回は境界で切る。
  広告費とCVは広告日次シートから期間で切って自動計算する。手入力の上書きは廃止した
  （第1〜3回に入っていたがMeta分しか含んでおらず、Google/LINEのウェビナー配信が
  抜けていた）。
- **架電業者ぶんは「予約が立つ直前（同日を含む）にかけたのが業者か」で判定する。**
  業者アカウントは `config/callers.json`。**IDだけを書き、氏名は書かない**
  （公開リポジトリなので。IDと氏名の対応はHubSpot側で引く）。
  **以前は「誰が取引を作ったか」（`hs_created_by_user_id`）で見ていたが、
  2026-09-14 に変更した。** 業者がアポを取っても取引を社内が作ると業者の実績から
  落ちるため。実測で3件あった（Sawayaka Club・若草会・ウインググループ）。
  業者ぶんは6件→9件、商談化率は1.0%→1.6%になった。
  **取引作成のルールが守られているかに結果が左右されない**のが変更の理由。
  業者が取引を作らなかったケースは集計ログに `[info]` で出るので、運用が直ったかも
  そこで分かる。
- **直前架電ベースは一度失敗して戻した経緯がある。今回は再現しないことを実測で
  確認してから変えた。** 当時はコールの `hs_call_direction` が空のものを発信と
  判定できず落とし、業者が取った予約2件が「架電なし」に回った。今はその欄が空でも
  発信として数えている。変更前に両方式を並べて数え、**「作成者=業者なのに
  直前架電=なし／社内」は0件**だった。判定を再び変えるなら、同じ比較を先にやること。
- **日付（`VENDOR_START` 以降）で業者ぶんとみなしてはいけない。** 日付起点の
  架電業者カードは廃止した（アカウント基準の「架電業者の週次」に置き換えた）。
  日付で切る作りは、業者アカウントしか架電していない期間だから成立していただけで、
  社内が1件でも架電すれば崩れる。
- **作成者別の母集団はFS指標と同じ**（直契約・route有効・獲得週が期間内・`PERIOD_START`〜）。
  業者作成＋社内作成＝FS活動量の同じ指標になる。
  コンタクト未紐付けの取引は作成者だけなら判定できるが、`route` が引けず直契約か
  代理店かを決められないので、ここでも母集団から外す（他のセクションと同じ扱い）。
- **「社内 作成」は「社内が架電して取った」という意味ではない。** ウェビナーやフォームから
  来たリードを社内が入力しただけのものも入る。架電を経たかどうかはこの区分では分からない。
  **だから全体の商談化率（面談予約÷架電数）は出さない。** 一度出してみたら、
  2026-08-10の週が架電1件・面談予約3件で300%になった。この割り算が成立するのは
  業者側だけ（業者は架電しかせず、その架電から取った予約しか作らない）。
- **「取引作成者別」のセクションは廃止した。** 見たいのは業者ぶんで、それは
  IS活動量の「架電業者の週次」に入っている。`data.json` の `is_attr` は
  そちらと `validate_is_attr` で使い続けているので消さない。
- **架電件数を 消化／着手中 に割る基準は「コール結果一覧」のF列**
  （業者運用のコール成果ラベル。1-x〜4-xの段階になっている）。
  消化＝追い切りラベルが1件でも付いた（`FINISHED_DISPOSITIONS`＝番号不備・
  番号間違い・受付拒否・5回架電・アポイント獲得 新規/FC・明確なNG）／
  通算3コール到達（`CALL_CAP`）／商談に進んだ、のいずれか。
  **ラベルを「1件でも」で見るのは、アポ獲得のあとに追いかけ電話で「再架電」が
  付くと、最新だけ見たときに着手中へ戻ってしまうため。**
  3コールの例外が `NO_CAP_DISPOSITIONS`（リーチ ヒアリングなし・FC リーチ
  ヒアリングなし・日程調整URL送付）。F列が「3コール」ではなく「時期開けて架電」
  「フォローコールで再確認」なので、回数に関係なく着手中のまま。
  **日程調整URL送付を消化に入れると、予約直前の追客中が在庫から消える。**
  こちらは最新のコール結果で見る。
- **未消化残高は「まだかけていなくて、これからかける相手」。2つ外している。**
  電話番号がどこにも無い人（`PHONE_PROPS` の5項目すべて空。かけようがない）と、
  `架電除外`（`kadenjogai`）が立っている人（かけられるが、かけないと決めた）。
  **`phone` と `mobilephone` だけ見てはいけない。** `代表番号(検索)` に
  1,300件以上入っていて、そこだけ見ると「かけられない人」が6倍に出る
  （一度1,036件と誤った。正しくは167件）。
  **メール配信を停止した人は架電除外に入れる方針**（2026-09-14 決定。117人に設定）。
  消化には入れない。消化は「追い切った」で、架電除外は「そもそもやらない」。
  混ぜると架電チームが実際より仕事をしたように見える。
- **`config/deal_date_overrides.json` は取引の日付の手動上書き。**
  HubSpotのステージ入り日（`hs_v2_date_entered_*`）は読み取り専用で書き換え
  できないので、実態と違う日付が記録された取引だけをここで直す。
  上書きしたものは集計ログに出る。**件数が増えてきたら運用側（成約したその日に
  登録する）を直すこと。**
- 介護校の架電（`CRM_UI`）は全期間で600件弱、**最初が 2026-05-12**。2025年9月〜
  2026年4月は1件も無い。5月以前のコールが68,130件あるのは全部 `INTEGRATION`
  （Kintone移行で入った介護施設運営側）で、介護校のものではない。
  `CALL_SOURCE=CRM_UI` で除外している。
- **FS指標は直契約のみ**（`ch != "agency"`）。全期間にすると上部の総商談数と一致する必要がある。
- **面談予約日が無い取引は、面談実施日を面談予約日として使う。**
  相談申込ステージを通っていない取引が14件ある（全部2025-09〜2025-12）。当時は
  相談申込を飛ばして「相談済み」から取引を作っていた。ステージ自体は2025-10-31から
  存在するので、無かったのではなく運用が不統一だっただけ。面談まで進んでいるのに
  面談予約0件として扱われ、2025年の予約率が実態より低く出ていた。
  面談したなら予約はあったはずで、記録が残っていないだけ。2026年は全件が相談申込を
  通っているので影響しない。この2つが同日になるが、ファネルに「予約→実施率」の行が
  無いので画面に不自然な100%は出ない。
- **日付の軸は2つある。絶対に混ぜない。**
  - **コホート軸（コンタクトの実効獲得日）**… `direct` / `agency` / `direct_day` /
    `agency_day`。leads・appts・deals・won をすべて「そのリードを獲得した日」に載せる。
    率の分子と分母が同じ集団になるのはこの軸だけなので、**転換率はここで出す**。
  - **イベント軸（できごとが起きた日）**… `calls` / `fs` / `is_attr`。
    「その月に何件やったか」という活動量。**IS・FSの活動量はここで出す**。
  - 面談予約は両方の軸に存在する（`direct[*][ch]["appts"]` と `calls[*]["appts"]` /
    `is_attr`）。名前が同じでも別物。**片方を分子、もう片方を分母にしてはいけない。**
    やると「7月に獲得したリード798件」÷「7月に入った予約10件」のように、
    別の集団を割ることになる。
  - 直契約の面談予約は両軸で合計が一致するはず。`validate_is_attr` が突き合わせている。
- 商談はコンタクト獲得週のコホート、FS指標はステージ到達日ベース。
- 率は加重（合計÷合計）。週ごとの率を平均しない。

主な定数: `PERIOD_START=2025-09-01` / `CALLS_START=2026-06-01` /
`VENDOR_START=2026-08-19`（架電業者稼働開始）。広告データの開始日は定数で持たず、
シートに入っている最初の日をそのまま使う（2026-04-07）。

## 表示の決めごと

- **分析コメント・注釈・改善提案は書かない。数字だけ。**
- **表の向きは中身で決まる。迷ったらこの区別に戻る。**
  - **時系列は期間を横**（月次ファネル / Metaキャンペーン別 / web広告の週次）。
    左から右に流れを追う。指標を縦に置き、指標名と累計列を左に固定する。
  - **並列な選択肢は項目を縦**（ウェビナー別 / 展示会別）。「どのお題が良かったか」を
    探すので、1つの列を上から下に見る動きになる。項目は増え続けるため、
    横に置くと列が無限に伸びる。名前も長い（「第8回 介護記録」）。
  - 月次テーブルは `month_table()`、週次は `week_table()`（どちらも `period_table()`）。
- 用語: 転換率ではなく**商談化率**。面談予約は**面談予約 獲得数**。
- インサイドセールス（架電）とフィールドセールス（面談）は別セクション。
- **「重要値」は営業シミュレーター（GASの別アプリ）の入力欄に対応した枠。**
  ヘッダーの直下に置く。3段＝単価と転換率／架電業者／消化状況。
  **項目はシミュレーター側の入力欄と定義を揃える**（CPL＝リード単価、
  商談化率＝架電→商談、成約率＝**実施**→成約、単価＝平均成約単価）。
  **成約率の分母は面談実施（イベント軸・`fs`）で、面談予約ではない。**
  シミュレーターの商談枠はクローザーの処理能力（1人目30件/月・専任50件/人）
  なので、向こうの「商談」は実施した面談を指す。予約が入っただけでは枠を
  消費しない。このダッシュボードの「商談」は相談申込ステージの取引＝面談予約で
  必ず同数になる（予約78・実施58）。予約を分母にすると 7.7%、実施なら 10.3%。
  **ヘッダーの成約率（予約ベース）と重要値の実施→成約は数字が違って正しい。**
  行の名前を「商談→成約」ではなく「実施→成約」にしてあるのはそのため。
  シミュレーター側の注記が「商談化6.0%＝架電済リードの4.0/66.4」なので、
  **架電→商談の分母は架電件数**（追い切った件数ではない）。
  **追い切った人だけを分母にした商談化率は出さない。** アポが取れた人は
  商談ありの条件で必ず消化に入り、取れなかった人はラベルか3コールに届くまで
  着手中に残るので、分母に成功者だけが先に溜まって率が上振れする
  （実測 3.9% に対して 16.3% と出た）。
  **広告費と社内ぶんの架電件数はシミュレーターの入力にあるが、この枠には
  入れない**（2026-09-16 にユーザーが不要と判断。月次KPIとIS活動量にある）。
  **架電対象（＝架電件数＋未消化残高）はこの枠が初出。** 消化の分母。
  **消化状況の4枠と平均成約単価は全期間固定**（id を振らず、期間フィルタで
  動かさない）。消化状況は「今この瞬間のリスト在庫」で、期間で切ると
  **1枠の中で軸が混ざる**。架電業者はイベント軸（その期間に何件かけたか）、
  消化状況はコホート軸（その期間に獲得したリードの進み具合）なので、
  9月に絞ると「業者は177件かけたのに架電対象は50件」と並んで読み違える。
  平均成約単価は成約が6件しかなく、期間を絞ると消える。
- セクション順: 重要値 → 月次KPI → リード獲得 → ウェビナー別 → IS活動量 → 月次実績
  → 直契約 → 展示会別CPL → チャネル別
- **月次の表は2つあり、軸が違う。混ぜないこと。**
  - **月次KPI**（獲得月ベース・コホート軸）… 「あの月に取ったリードはどこまで
    進んだか」。**率を出せるのはこちらだけ**（分子と分母が同じ集団になる）。
    追う4指標（CPL・リード数・消化＝未消化残高・架電→商談／商談→成約）を
    1本の鎖にしてある。
  - **月次実績**（できごとが起きた月ベース・イベント軸）… 「今月どれだけ動いて、
    何件決まったか」。**月をまたいだ増減を見られるのはこちら。**
    元は「FS活動量」で、架電数・架電件数・面談予約を足して改名した。
  - 月次KPIでは「今月何件成約したか」が読めない。9/4の成約は8月に獲得した
    リードなので8月の列に載る。だから月次実績が要る。
- **月次KPIの前月差は 広告費・リード数・CPL の3行だけ。** 列は「その月に獲得した
  リードの集団」なので、隣と比べているのは別の大きさ・別の熟成度の集団になる。
  26/7→26/8 の架電件数 267→46 は、8月のリードが7月の5分の1（798→160）という
  だけで架電の動きを何も表していない。下の段はさらに経過時間の差が乗る。
- **消したセクション・表（戻す前に理由を読むこと）**
  - **代理店セクション**（グラフ2枚＋週次テーブル）… 全期間でリード24件で、
    53週のうち19週にしか値が無い。商談化率はリードがそのまま商談になるため
    ほぼ全週100%、成約率は1件の増減で0%と100%を行き来していた。
    数字はヘッダーのカードに残っている。`compute_agency` / `totals_agency` と
    RAW の `al`/`ad`/`aw`/`aa` はそのカードの期間再計算に使うので消さない。
  - **直契約の週次テーブル**（53週 × チャネル別・319行）… 中身は上のグラフ4枚と
    同じで、週を縦に並べる古い向きだった。チャネル別の実数だけ
    「チャネル別」セクションに残した（率は入れない。LINE・紹介・その他は
    リードが3〜16件しかなく、1件の増減で0%と33%を行き来する）。
  - **取引作成者別セクション**、**IS/FSの日次テーブル**、
    **架電したリード → 面談予約**（週を縦・「転換率」表記）。
- **よく見る数字はカードで出しっぱなし、たまに見る数字は折りたたみの中。**
  折りたたみは既定で閉じる。開閉状態は localStorage で保持。
  サマリーには件数と主要な数字を出して、開く前に中身の見当がつくようにする。
- **カードは動きが毎日あるものだけ。** IS活動量は「本日」だけを出し、累計は
  週次テーブルに畳んだ。FS活動量は逆で「本日」を出さない（面談・提案・成約の
  どれかが起きた日は12ヶ月で48日しかなく、ほとんどの日が全部0になる）。
- **日次のテーブルは置かない。** 最小の粒度はISが週、FSが月。日次は行が数十〜百近く
  になるうえ、上のグラフと期間テーブルで同じ数字が読める。
- 期間指定を変えたら各カードの数値も再計算されること。

## 触ってはいけないこと

- **トークンやキーの発行・入力をこちらでやらない。** ユーザー本人が GitHub Secrets に入れる。
  中身を見せてもらう必要もない。
- 公開リポジトリなので、認証情報をコミットしない（`.gitignore` で名前を弾いている）。
- 公開ページから再集計を起動するボタンは作れない（トークンが露出する）。
